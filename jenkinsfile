//Jenkinsfile
//Run terraform init
stage('init'){
    ansiColor('xterm') {
        sh 'terraform init'
    }
}

//Run terraform plan
stage('init'){
    ansiColor('xterm') {
        sh 'terraform plan'
    }
}
